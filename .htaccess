# BookIT Apache Configuration - MASTER FILE
# Consolidates .htaccess and .htaccess.simple

# Disable directory listing
Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /BookIT/
</IfModule>